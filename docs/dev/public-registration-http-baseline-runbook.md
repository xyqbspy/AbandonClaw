# 公网开放真实 HTTP Baseline 操作手册（已并入）

本页不再单独维护。

原 runbook 的配置字段、dry-run / 全量执行顺序、`passed / blocked / failed` 判读与留证要求，已经并入：

- `backend-release-readiness-checklist.md` 的「真实 HTTP 验证」
- `release-readiness-assessment.md` 的 P0-2 与放行边界判断

保留本页仅为历史链接兼容。后续更新只改上述两份主文档。
