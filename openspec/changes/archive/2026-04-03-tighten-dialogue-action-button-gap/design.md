## Current Flow

当前对话气泡与移动端分组气泡下方按钮区都使用较大的按钮间距，导致翻译图标和播放按钮之间留白偏多。

## Decision

设计决策：
- 仅调整两个按钮容器的 `gap`
- 从当前较大的间距收紧到更紧凑的同级 spacing
- 不触碰按钮尺寸、顺序和点击行为

## Validation

验证方式：
- 检查两个气泡操作区按钮间距是否同步变小
- 运行乱码检查与 OpenSpec 校验
