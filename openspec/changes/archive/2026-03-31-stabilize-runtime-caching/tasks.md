## 1. 场景缓存刷新策略

- [x] 1.1 调整 `scenes` 列表加载逻辑，使其命中缓存后仍执行受控后台刷新，而不是在新鲜缓存命中后直接返回。
- [x] 1.2 调整 `scene detail` 加载逻辑，使其与 `scenes` 列表保持相同的新鲜度策略，并保留离线回退能力。
- [x] 1.3 重新审视 `scene-list-cache` 与 `scene-cache` 的 TTL、淘汰与预取边界，避免本地缓存长期遮蔽服务端更新。

## 2. 缓存职责统一

- [x] 2.1 统一 `/api/scenes`、`/api/scenes/[slug]`、`/api/phrases/mine` 等用户态 GET 接口的响应缓存语义。
- [x] 2.2 统一相关客户端 `fetch` 的缓存选项，避免默认浏览器缓存与自定义缓存层职责重叠。
- [x] 2.3 对 `chunks` 列表、`scenes` 列表和 `scene detail` 的缓存失效规则做一次交叉核对，确保短语变更、学习状态更新和手动刷新行为一致。

## 3. 场景音频预热

- [x] 3.1 在现有 `tts-api` 持久缓存体系内接入整段场景循环音频预热，不新增独立缓存层。
- [x] 3.2 约束场景整段音频预热的触发入口、空闲调度和弱网保护，避免造成无控制的额外带宽消耗。

## 4. 验证与文档

- [x] 4.1 补充或更新 `scenes` 列表、`scene detail`、`chunks` 列表与音频缓存相关测试，覆盖缓存命中后后台刷新、请求去重和预热行为。
- [x] 4.2 跑通受影响链路的测试命令，并记录已验证范围与未覆盖风险。
- [x] 4.3 在实际实现完成后更新根目录 `CHANGELOG.md`，记录用户可感知的缓存与音频体验改动。
