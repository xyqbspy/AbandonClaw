## 1. 正式信号范围与数据模型

- [x] 1.1 盘点当前 `review` 递进式阶段、现有 `again / hard / good` 提交和场景回补正式记录，明确哪些新阶段结果值得升级为正式后端信号。
- [x] 1.2 设计 `review` 正式信号的数据模型，区分事件层与聚合摘要层，并明确与现有 review 状态字段的关系。
- [x] 1.3 明确历史数据迁移策略，约束新增信号如何对历史记录保持保守解释而不是强行回填。

## 2. API / Service / 聚合链路

- [x] 2.1 设计并实现 `review` 提交 API、服务端 service 或 SQL 的增强方案，使正式信号能够稳定记录。
- [x] 2.2 明确新信号如何影响复习调度、统计和结果解释，并保证不会破坏现有 `again / hard / good` 兼容性。
- [x] 2.3 评估并实现 `today` / dashboard 等聚合入口对 review 正式信号摘要的消费方式。

## 3. 文档、测试与验证

- [x] 3.1 补充 `review` 正式信号专项维护文档，说明字段边界、聚合摘要和消费规则。
- [x] 3.2 为 API / service / selector / 聚合链路补充回归测试，覆盖新旧信号并存和历史记录兼容。
- [x] 3.3 跑通受影响测试与 `openspec` 校验，并在实际改动后更新 `CHANGELOG.md`。
