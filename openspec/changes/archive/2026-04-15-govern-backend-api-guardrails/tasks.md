## 1. 公共治理基线

- [x] 1.1 在 `middleware.ts` 和公共服务端工具中引入统一 `requestId` 透传机制。
- [x] 1.2 新增统一日志 helper，约束接口错误日志至少包含 `requestId`、路径和错误上下文。
- [x] 1.3 扩展 `src/lib/server/api-error.ts`，让未知内部错误响应稳定收口并返回 `requestId`。

## 2. 接口入口保护

- [x] 2.1 新增最小同源来源校验 helper，并接入受保护写接口。
- [x] 2.2 新增进程内限流 helper，并接入高成本接口。
- [x] 2.3 为高成本接口补齐统一的输入规模限制与拒绝策略。

## 3. 高风险接口接入

- [x] 3.1 为 AI / 解析 / TTS 相关接口接入 requestId、限流和统一错误收口。
- [x] 3.2 为核心写接口接入来源校验，并确认不破坏现有业务主链路。
- [x] 3.3 评估并记录本轮未纳入第一阶段接入的接口及残余风险。

## 4. 验证与文档

- [x] 4.1 补充 middleware、api-error、限流、来源校验和关键 route 的自动化测试。
- [x] 4.2 运行受影响的最小测试集合并记录结果。
- [x] 4.3 更新 `CHANGELOG.md` 和相关开发记录，说明本次接口治理基线变更。
