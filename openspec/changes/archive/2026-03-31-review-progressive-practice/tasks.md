## 1. 递进式复习模型与页面状态

- [x] 1.1 盘点当前 `review` 页普通表达复习与场景回补的阶段模型，明确哪些现有步骤可以映射为微回忆、熟悉度判断、变体改写和完整输出。
- [x] 1.2 设计并实现递进式复习状态机/selector 映射，支持按复习项类型与掌握度决定训练深度，而不是所有题都走满相同流程。
- [x] 1.3 为失败路径设计稳定降级策略，明确何时回到提示、何时回到原场景继续练、何时允许直接进入下一题。

## 2. 后端记录与 TODO 边界

- [x] 2.1 评估现有 `review` API / service 是否足以承载新增阶段信号，明确哪些阶段结果暂时只做前端 TODO，哪些需要正式后端字段或事件。
- [x] 2.2 如果需要后端增强，补充对应 API / service / SQL 计划或实现，确保不会把未接通能力伪造成正式学习信号。
- [x] 2.3 校准 `today`、dashboard 或其他聚合入口是否需要消费新的复习阶段解释，避免页面之间对 `review` 能力的解释不一致。

## 3. 文档、测试与验证

- [x] 3.1 补充 `review` 递进式练习专项维护文档，写清阶段定义、回写边界和失败降级规则。
- [x] 3.2 为递进式阶段推进、分层路径和失败降级补充逻辑/交互测试。
- [x] 3.3 跑通受影响测试、`openspec` 校验，并在实际改动后更新 `CHANGELOG.md`。
