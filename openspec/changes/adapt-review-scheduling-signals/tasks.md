## 1. 调度规则与数据模型

- [x] 1.1 盘点当前 `review` due 列表排序、`next_review_at` 更新规则和正式训练信号字段，明确哪些信号会影响优先级，哪些信号会影响复习间隔。
- [x] 1.2 设计“最终反馈定大方向、正式信号做细调”的调度规则，明确不同组合下的优先级和时间间隔。
- [x] 1.3 明确历史空值记录的兼容策略，约束旧记录在新调度下如何被保守解释。

## 2. 服务端实现与页面解释

- [x] 2.1 实现 due 列表排序增强，让低输出信心、未完成完整输出、仅识别级别的条目按约定优先出现。
- [x] 2.2 实现 `next_review_at` 或等效节奏字段的细调策略，确保正式信号真正影响下一次复习时间。
- [x] 2.3 评估并更新 `review` / `today` 的相关说明文案或摘要字段，让页面能稳定解释新的调度节奏。

## 3. 文档、测试与验证

- [x] 3.1 补充 `review` 调度策略专项维护文档，说明排序规则、节奏规则和兼容边界。
- [x] 3.2 为调度规则、due 列表顺序、间隔更新和页面解释补充回归测试。
- [x] 3.3 跑通受影响测试与 `openspec` 校验，并在实际改动后更新 `CHANGELOG.md`。
