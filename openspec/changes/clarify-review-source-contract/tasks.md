## 1. 来源契约与后端链路

- [x] 1.1 盘点 `review` 普通表达复习与场景回补的当前后端数据来源、场景关联字段和原场景跳转链路。
- [x] 1.2 设计并实现“查看原场景”在来源场景失效时的稳定降级策略，必要时补充服务端可访问性字段或前端校验逻辑。
- [x] 1.3 明确实现后 `review` 普通表达复习的来源契约仍为“已保存且到期的表达”，避免隐式引入“仅已完成场景”的过滤。

## 2. 页面与交互维护

- [x] 2.1 更新 `review` 页面相关 selector / labels / page 逻辑，确保原场景入口只在满足契约时展示。
- [x] 2.2 为原场景失效、无来源场景和正常可跳转三种情况补充交互或逻辑测试。
- [x] 2.3 评估 `today` 或其他入口是否需要同步调整来源说明，避免页面之间对 review 来源的解释不一致。

## 3. 文档与验证

- [x] 3.1 新增 `review` 专项维护文档，说明展示数据、后端关系、来源规则和降级策略。
- [x] 3.2 更新对应 OpenSpec spec delta，确保来源契约和原场景降级规则可归档。
- [x] 3.3 跑通受影响测试与 `openspec` 校验，并在实际改动后更新 `CHANGELOG.md`。
